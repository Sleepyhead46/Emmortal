import Link from "next/link";
import { ArrowUpRight, LockKeyhole, QrCode, Download, ShieldCheck, Server } from "lucide-react";

const features = [[QrCode, "QR Generator", "Create beautiful codes locally in your browser."], [Download, "AnyDownload", "A focused interface for public media workflows."], [LockKeyhole, "Privacy first", "No accounts, trackers, or unnecessary collection."], [ShieldCheck, "Secure by design", "Validation and network boundaries belong at the edge."], [Server, "Cloud ready", "A clean foundation for dependable deployment."]];
export default function Home() { return <main>
  <section className="overflow-hidden border-b border-neutral-200"><div className="container py-28 md:py-40"><p className="eyebrow fade-up">Tools for the open web</p><h1 className="fade-up mt-5 max-w-4xl text-6xl font-semibold tracking-[-0.08em] md:text-8xl">Simple tools.<br /><span className="text-neutral-400">Powerful results.</span></h1><p className="fade-up mt-8 max-w-xl text-lg leading-8 text-neutral-600">Emmortal is a quiet workspace for creating QR codes and working with publicly accessible media. Useful by default, private by design.</p><div className="fade-up mt-10 flex flex-wrap gap-3"><Link href="/qr" className="inline-flex items-center gap-2 rounded-full bg-black px-5 py-3 text-sm font-semibold text-white">Create QR Code <ArrowUpRight size={16} /></Link><Link href="/anydownload" className="inline-flex items-center gap-2 rounded-full border border-neutral-300 px-5 py-3 text-sm font-semibold">Open AnyDownload <ArrowUpRight size={16} /></Link></div></div></section>
  <section className="container py-20"><div className="flex items-end justify-between gap-6"><div><p className="eyebrow">The toolkit</p><h2 className="mt-3 text-3xl font-semibold tracking-[-0.05em]">Small surface area.<br />Thoughtful details.</h2></div><span className="hidden text-sm text-neutral-500 md:block">01 — 05</span></div><div className="mt-10 grid gap-px border border-neutral-200 bg-neutral-200 dark:border-neutral-800 dark:bg-neutral-800 sm:grid-cols-2 lg:grid-cols-5">{features.map(([Icon, title, copy], index) => <div key={title as string} className={`${index === features.length - 1 ? "sm:col-span-2 lg:col-span-1" : ""} bg-[#fafafa] p-6`}><Icon size={20} strokeWidth={1.5} /><h3 className="mt-12 font-semibold">{title as string}</h3><p className="mt-2 text-sm leading-6 text-neutral-500">{copy as string}</p></div>)}</div></section>
</main>; }
